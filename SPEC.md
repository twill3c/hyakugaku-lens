# SPEC.md — hyakugaku-lens(百学レンズ)

<!-- scaffold template v1.24.0 から展開(2026-08-31)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

AI 研究者ではない立場から **AI がもたらす未来を研究し発信している各界の学者 100 名**
(海外 80 名・日本 20 名)について、紹介文と発信の最新状況を一望できる単一 HTML
ダッシュボードを提供する。姉妹プロジェクト [hyakunin-lens](https://hyakunin-lens.vercel.app)
が AI の**作り手**を扱うのに対し、本プロジェクトは AI を**論じる側**を扱う。

hyakunin-lens との決定的な違いは、**初期データが人手収集のスナップショットではないこと**である。
名簿(氏名・所属・専門・紹介文)だけを人が書き、発信の項目は**すべて機械収集で埋める**。
収集できなかった人は 0 件のまま出す。これは網羅性を捨てて**再現可能性**を取る設計判断である。

## 2. 機能要求

| ID | 要求 | 優先度 | 状態 |
|---|---|---|---|
| F-01 | `data/people.json` に 100 名分のレコード(n/en/c/aff/field/h/bio/own/pub/yt)を保持する。分類は 12 区分、うち日本の学者 20 名 | must | loop_001 |
| F-02 | `src/build.py` が data + `src/template.html` から自己完結の `out/index.html` を生成する(外部リソース参照なし) | must | loop_001 |
| F-03 | ページは分類絞り込み(12 区分)・全文検索・セクション切替(本人の発信/学術発表/講演・対談)・「直近1ヶ月」「直近1週間」絞り込み(閲覧時点基準・排他)を提供する | must | loop_001 |
| F-04 | 件数行はデータから算出して埋め込む(手書きの数字を持たない) | must | loop_001 |
| F-05 | 日付「不明」の項目は淡色表示し、期間絞り込みの対象外とする | must | loop_001 |
| F-06 | `data/sources.json` に人物ごとのフィード取得経路を宣言的に保持する。**実際に取得し、本人のものだと言える証拠が取れたものだけ**を載せる。証拠は item-author / own-domain / feed-title の三種で、自動判定に落ちるものは `data/feed_declared.jsonl` に理由を書いて例外として通す | must | loop_002・21 本 |
| F-07 | `src/update.py` が宣言フィードを収集し、成功したソース種別の項目だけを差し替えて宣言された欄を再構成する(他種別・失敗時は既存維持=劣化継続)。各セクション最大 3 件・日付降順(不明は末尾)。CMS の初期投稿は収集の成否によらず落とす。**絞り込みで落とした件数は理由ごとに数えて記録に出す** | must | loop_002 / loop_010 |
| F-08 | `src/scholar.py` が OpenAlex API から最近の学術発表を取得して pub を更新する。著者 ID は `data/scholar_ids.json` に**二要素(所属 × 分野)で同定したうえで**固定で持ち、実行時の氏名検索で解決しない | must | loop_003 |
| F-09 | GitHub Actions cron が update / scholar を定期実行し、差分があるときだけ `data/` + `out/` をコミットする(Vercel Git 連携で自動デプロイ) | must | loop_004・連携済み |
| F-10 | フッタはフリート共通規約(MIT License © ・ GitHub ・ 歩き方 ・ 設計図 ・ App Menu の単一段落)で画面最下部に固定表示。ヘッダーに最終更新時刻(JST)と自動更新の層ごとの最終実行日を表示する | must | loop_001 |
| F-11 | 講演・対談(yt)を二経路で更新する。①**本人が主宰するポッドキャスト**(Apple の検索で発見し中身を読んで宣言)②**YouTube**(`data/yt_channels.jsonl` の(チャンネル × 人物)許可リストを通ったものだけ)。取得元は入れ先の欄(`sec`)を宣言できる。審査ファイルは今日検索しなかった人の記録を持ち越し、育てた許可リストは `--from-review` で取得済みの候補に当て直せる | should | loop_010 / loop_012 / loop_014 |
| F-12 | フィードの探索は公式サイト自身のフィードに加えて、公式サイトが**自分で掲げている外向きリンク**(Substack / Medium / note / Mastodon)を候補にする。候補は候補でしかなく、採用条件(F-06 の証拠)は変えない。題名を持たない投稿(Mastodon の RSS)は本文の冒頭 80 文字を題名にして採る | should | loop_013 |
| F-13 | 0 件の欄には「確認できず」の一語ではなく、**収集の記録から導いた理由**を出す(取得元なし / 取得元 N 本・現在 0 件 / 取得できず / 同定できず / 対象外 / 審査待ち N 件 / 未検索)。理由の語彙は `src/build.py` の `REASONS` に閉じ、各文が記録上の述語に対応する。出力の `WHY` はデータから再計算した結果と往復一致する | should | loop_015 |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | ビルドは決定的(同じ data から同じバイト列) | T-05 |
| N-02 | 全テキストは HTML エスケープして埋め込む(XSS・表示崩れ防止) | T-04 |
| N-03 | Vercel に `out/` をそのまま静的配信(buildCommand なし) | vercel.json |
| N-04 | 日本語の本文(紹介文を含む data/*.json)に別字種・制御文字が混入していない | T-11 |
| N-05 | 繰り返し取得する経路は、取りに行く前に robots.txt で可否を確かめる。出荷しているフィード・公式サイト URL がすべて許可されていることを実測で押さえる | T-19 |

## 4. 品質基準

- ビルド出力から埋め込み JSON を再抽出すると `data/*.json` と完全一致する(往復一致オラクル)
- 件数は**定数で書かない**。人数 100 と分類の内訳だけを固定し、項目数は算出制にする(HC-016)
- 日付は `YYYY-MM-DD` / `YYYY-MM` / `不明` のみ。URL は http(s) のみ。分類・ソース種別は語彙表に閉じる
- 公式サイト URL(`h`)は `tools/verify_links.py` で**実際に取得して確認した**ものだけを載せる。
  確認できなかった人は空文字で出す(推測した URL を書かない)

## 4.1 フィードを本人のものと言うための証拠(F-06 の実測 2026-08-31)

590 本のフィードを実際に取得して判定した結果、**取得できることは本人のものであることを
意味しない**という失敗が二種類出た。採用条件はこの二つから導いている。

| 証拠 | 意味 | なぜ足りるか / 足りないか |
|---|---|---|
| item-author | 記事の著者名に本人の名前が入る | 最も強い。共著ブログでも本人の記事だけを選り分けられる |
| own-domain | フィードの host が公式サイトと同じ**かつ**公式サイトがその host の根にあるか経路が下にある | ホスト一致だけでは足りない —— 公式サイトが大きなサイトの 1 ページのとき、MIT 経済学部・コーネル法科大学院・OII・CEPR・note.com の**サイト全体のフィード**が本人の発信として 8 件混入した |
| feed-title | フィードの表題に本人の名前が入る | 単著フィードでのみ有効。共著ブログ Balkinization は表題に本人名を含むが記事は 6 名のもの |
| declared-* | 自動判定に落ちるが中身を実測して人が採ると決めた | `data/feed_declared.jsonl` に **note 必須**。現在 3 本(自動より少ないことをテストで固定) |
| declared-host | 本人が主宰する番組(ポッドキャスト)。`sec: "yt"` で講演・対談の欄へ入れる | Apple のポッドキャスト検索で発見し、**回の題名を読んで**本人のものだと確かめる。氏名の一致だけでは決められない —— 同名の別人の番組が実在した(ヘンリー・ファレル名義のパブリックドメイン朗読番組、25 話) |
| standing-author | 多著者の論説媒体(Project Syndicate / Aeon / Noema)を常設の取得元にする | 確かめるのは「その媒体が項目ごとに著者名を持つこと」。本人の記事が**今**載っているかとは独立に成り立つ性質で、載っていない日は 0 件になり既存が残る。誤帰属の危険は無い —— 著者名が一致した項目しか採らない |

**著者名は書き手とは限らない。** ハラリとゴールディンの公式サイトでは `dc:creator` が
運営スタッフのアカウント(Dima Basov / Sami Wymes)で、記事そのものは本人のものだった。
逆に Balkinization では `dc:creator` が本当に別々の学者だった。両者を分けるのは
機械では難しく、ここは実測して人が決める場所として残してある。

**別人を返すフィードが実在する。** The Conversation の著者 Atom は数値 ID だけで解決され、
スラグ部分は無視される。`mark-coeckelbergh-1149861` は HTTP 200 と妥当な Atom を返しながら
別人(Leping Mou)のフィードだった。この経路は本プロジェクトでは使わない。

## 4.2 著者を同定するための二要素(F-08 の実測 2026-08-31)

氏名検索だけでは決めない。**所属が一致しても別人**という実例がある ——
「Takehiro Ohya @ Keio University」は慶應の法哲学者ではなく、映像符号化を研究する
工学者だった。両者は所属で区別できず、**トピックのドメイン**で分かれる。

| 要素 | 条件 | 落ちる例 |
|---|---|---|
| 所属 | 宣言した機関名の一部が `last_known_institutions` か `affiliations` に現れる | 同名の別分野の研究者(所属が違う) |
| 分野 | 著者のトピックが Social Sciences か Arts and Humanities のドメインを含む | 工学者の T. Ohya(Physical / Health のみ) |

両方を満たす候補が複数残ったら業績数の多い方を採り、`ambiguous` として記録に残す。
どちらも満たす候補が無ければ**その人は pub の対象外にする**(推測で当てない)。
所属を持たない独立研究者(エリック・サダン)は宣言そのものを作らない。

**OpenAlex には日次の無料予算がある(実測 2026-08-31)。** 1 リクエストあたり $0.001 で、
使い切ると 429 と `Retry-After`(UTC 深夜までの秒数)を返す。初回の同定は候補を個別に
引き直していたため 1 人あたり最大 9 リクエストかかり、99 名の途中で予算が尽きた。

その結果、**一覧の末尾にいた日本の学者 19 名は検索そのものが落ち**、しかし出力は
「二要素を満たす候補なし」だった —— 取れなかったのか該当しなかったのかが区別できず、
危うく「日本の学者は OpenAlex に載っていない」という誤った結論を出す寸前だった。
記録は `verified` / `no_match` / `unreachable` の三分割で持ち、合計が宣言数に一致することを
テストで固定している(2026-08-31 時点で 74 / 6 / 19)。

取得回数は `select` で判定に要る欄をまとめて取れば 1 人 1 リクエストに減る**見込み**である。
実装は入れたが、予算切れのため当日は実測できていない —— 次回の自動更新で
`data/scholar_ids.json` の `unreachable` が減るかどうかで確かめる。

## 4.3 取得してよいかを先に確かめる(N-05 の実測 2026-08-31)

**関門が無い間は、違反も 0 件に見える。** 本プロジェクトは 3 ループのあいだ
robots.txt を確認せずに取得していた。関門(`src/robots.py`)を作って過去に当てたところ、
その場で 3 件の違反が出た。

| 見つかったもの | 経緯 |
|---|---|
| researchmap の氏名検索 `/researchers?q=` | loop_001 で 19 回叩いた。道具は使用停止にし、得た ID は許可されている公開プロフィール直下で確かめ直した |
| NYT の署名コラム RSS(`/svc` 配下) | loop_002 で採用。取得元と、その経路で得た 3 件を取り下げた |
| Rotman(トロント大)の教員ページ | 公式サイト URL として出荷していた。空欄にした |

判定は自前で行う。標準ライブラリの `urllib.robotparser` は経路の**途中**のワイルドカードを
解釈せず、`Disallow: /*/misc/` を素通しにする —— researchmap の robots.txt はこの形を
18 行使っており、既製品では関門を張ったつもりで穴が空く。

**取れなかったことを「禁止」と読み替えない。** robots.txt が落ちる理由はたいてい
ネットワーク側にあり、そこで全経路を止めると収集が静かに全滅する。取れなかったホストは
記録して通す。

## 4.4 落としたものを数える(F-07 の実測 2026-09-01)

**黙って捨てる絞り込みは、欠落をデータの不在に化けさせる。** 取り込み関数が
「item に link が無い」項目を捨てていたところ、ポッドキャストのフィードで
336 件中 191 件が落ち、**活動中の番組が 2 年前で更新の止まった番組として画面に出た**。
落ちたのが新しい回ばかりだったからである。

現在は `feed_items(..., dropped=)` が理由ごとの件数を返し、`data/update_status.json` と
実行ログに出る。回ごとのページを持たないフィードは番組ページへ退避し、
重複排除の鍵を **URL と見出しの組**にした(同じ番組ページを指す別々の回を畳まないため)。

## 4.5 YouTube は「場」で絞る(F-11 の実測 2026-09-01)

鍵を入れて実際に測った。**題名の字面では「本人による」と「本人についての」を分けられない。**

| 規則 | 候補 | 混入の見立て |
|---|---|---|
| 姉妹プロジェクトの較正をそのまま移植(**表示名で検索**) | 39 件 | 約 85%。「ペーター=ポール・フェルベーク」の表示名から取った「ペーター」が『あつ森の住民』『連続殺人犯ペーター・キュルテン』『聖ペーター教会』に当たり 22 件すべて別物。「山本勲」はプロボウラーの同名者 |
| **原綴(`en`)で検索**(日本の学者は漢字名) | 109 件 | 約 40%。カタカナ由来の誤りは消える |
| + 出演の型あり / 解説の型なし | 71 件 | 分離できず。公の場に出ないビョンチョル・ハンの解説動画が残る |
| チャンネル名に本人名(本人のチャンネル) | 7 件 | 7 件中 2 件がファンの解説チャンネル |
| **(チャンネル × 人物)の許可リスト** | 57 件 | **読んだかぎり混入 0**。ハンは 0 件になる —— 本人が動画に出ないので、それが正しい |

分けられるのは**場**である。`data/yt_channels.jsonl` に「この局はこの人を招いている」と
確かめた組だけを置き、許可外の候補は捨てずに `pending` として審査に回す。
許可リストはそこから育つ。各行は `kind`(own / interview / institution / event / press)と、
**判断の根拠にした実際の題名**を持つ。

**チャンネル単位では足りない。** ある人を招く場が別の人も招くとは限らない。実測:
マッカスキルの回で許可した `Zenvora Productions` が、**同名の別人**(柔術コーチの
ジョン・ダナハー)の回を 3 件通した。エピソード番号から、その局は有名ポッドキャストの
**非公式再アップを集約するチャンネル**だと分かった。許可は組で持ち、その局は外した。

運用: `collect-youtube.yml` が毎日 1 回、許可リストを適用して更新する(1 日 50 名・
2 日で一巡・5,000 units で無料枠の 50%)。手動実行は既定が審査モードで、
`data/yt_review.json` に候補を全部出す —— 許可リストを育てるための経路である。

**審査の材料は半分しか見えていなかった(実測 2026-09-06)。** 審査ファイルは毎日の実行で
上書きされ、その日の組(50 名)の候補しか残らない。前日の組の pending は git の履歴にしか
無かった。いまは今日検索しなかった人の記録を前回の審査ファイルから持ち越し
(`carry_over`・最初に得た日付を `carried_from` に残す)、100 名ぶんが常に見える。

**許可リストを育てたら、翌日の検索を待たずに当て直す**(`python -m src.youtube --from-review`)。
取得済みの候補へ同じ(チャンネル × 人物)の関門を通すだけで、検索はしない(クォータを使わない)。

loop_014 の審査: 両組の審査待ち 504 件(100 名)を読み、**本人が話している回**だけを
236 組として許可した(52 → 288 組)。落としたのは、要約・朗読・オーディオブック・
ファンの解説(ハン・ハラウェイ・ズボフの候補はすべてこれ)、有名番組の非公式再アップ
(Zenvora / MindVoice —— エピソード番号で判る)、同名の別人(リチャード・ボールドウィンの
ATV 動画・アンドリュー・マカフィーの説教・山本勲のボウリング・スーザン・シュナイダーの気候講演)、
AI 生成の合成対話(「A Day in the Polis」)。当て直しで 60 名の候補が通り、
講演・対談が 1 件以上ある人は 16 → 70 名、三欄すべて 0 件の人は 12 → 4 名
(ハン・稲葉振一郎・鈴木貴之・岡本裕一朗)になった。

## 4.6 本人が自分で掲げているリンク先を候補にする(F-12 の実測 2026-09-06)

「本人の発信」が 0 件の人が 100 名中 80 名いた。公式サイトそのものにフィードが無い人でも、
本人が Substack や Mastodon へ**自分のサイトからリンクしている**ことがある。出荷している
公式サイト 94 件を実際に取得して外向きリンクを数えた。

| リンク先 | サイト数 | 扱い |
|---|---|---|
| Substack(購読の根、または記事) | 6 | `/feed` を候補にする。うち 3 本は**他人の** Substack(ブログロール・記事の引用)で、judge が「手がかりが無い」で落とした |
| Mastodon(`rel="me"` を含む) | 3(本人 2・研究プロジェクト 1) | `@user.rss` を候補にする。フィードの表題が本人名なので feed-title で通る。同じ `@user` の形でも Threads / TikTok(4 サイト)は Mastodon ではないので除く |
| note | 1 | `/rss` を候補にする(東浩紀の公式サイトは note 自体で、公開記事が 0 件のためフィードが空) |
| Bluesky のプロフィール | 12(うち 9 は表示名が本人、3 は所属機関のアカウント) | RSS を持たない。公開 API(`public.api.bsky.app`)は robots.txt が許可している —— 別の取得器が要る(未実装) |
| YouTube のチャンネル | 14 | チャンネル RSS は robots.txt が禁じている(§4.5)。許可リストの経路へ |
| X 37 / LinkedIn 25 | — | 取得経路が無い(§5) |

`--outbound-only` で外向きリンク由来の候補だけを試し、**本人の発信がいま 0 件の人**にだけ足す。
取得元を持つかで見ないのは、多著者媒体の常設(standing-author)や `skip` した
ブログしか持たない人が 0 件のまま残るからである(シンガー・ラザー・フロリディがそうだった)。

結果: 取得元 49 → 54 本、本人の発信が 1 件以上ある人 20 → 25 名(シンガー・ラザー・
マカフィー: Substack を item-author で / ワクター・フロリディ: Mastodon を feed-title で)。

**Mastodon の RSS は item に title を持たない**(20 件中 20 件)。従来の関門は
「title/link のある項目が無い」として捨てていた。本文(description)のタグを剥がした冒頭を
題名にする。題名も本文も無い項目だけが「題名なし」で落ちる。

**測って捨てた経路: researchmap の Web API。** `api.researchmap.jp/{id}/published_papers` は
認証なしで JSON を返し、robots.txt も禁じていない(日本の学者 19 名中 15 名に業績があり、
OpenAlex で同定できない 5 名の pub を埋められた見込みだった)。しかし
[Web API 利用規約](https://researchmap.jp/public/terms-of-service/WebAPI) 第 2 条が
**JST への利用申請と承認**を求め、第 7 条 3 項が未承認の利用を利用停止の対象にしている。
取れることと取ってよいことは別である(§4.3 と同じ)。申請しない限り使わない。

## 4.7 0 件の理由を記録から導く(F-13 の実測 2026-09-06)

「確認できず」は少なくとも三つの違う状態を一語で潰していた —— 取得元が無い /
取得元はあるが現在 0 件 / 取得に失敗。読者には区別がつかず、作る側も
「取得元を足せば埋まる人」と「足しても埋まらない人」を画面から読めなかった。

理由は表示用の文だが、それぞれ収集の記録に対する**述語**に対応させる(HC-079)。
語彙は `REASONS` に閉じ、自由文を返さない。出力に埋めた `WHY` は
データから再計算した結果と完全一致する(往復一致・T-29)。

| 欄 | 理由 | 述語(記録) | 2026-09-06 |
|---|---|---|---|
| own | 公式サイトが未確認で、取得元も無い | `h` が空 かつ own の取得元なし | 6 |
| own | 取得元なし(公式サイトにフィードが見つからない) | `h` あり かつ own の取得元なし(skip / sec=yt は数えない) | 53 |
| own | 取得元 N 本・現在は本人の記事なし | 取得元あり・`update_status` に error なし | 16 |
| own | 取得元 N 本が取得できず | 取得元あり・全部に error | 0 |
| pub | OpenAlex の対象外(所属を持たない) | `scholar_ids` に宣言が無い | 1 |
| pub | OpenAlex に同定できず(所属と分野の二要素を満たす候補なし) | 宣言あり・verified=false・error なし | 12 |
| pub | OpenAlex に照会できず(利用枠の回復待ち) | verified=false かつ error か fetch_failed | 0 |
| pub | 同定済み・直近の業績が取れていない / 業績の取得に失敗 | verified=true・`scholar_status` の error の有無 | 0 / 0 |
| yt | 審査待ちの候補 N 件(許可した場がまだ無い) | `yt_review` に pending あり | 7 |
| yt | 直近半年の検索に候補なし(題名に氏名を含む長尺動画が無い) | 検索済み・pending も candidates も無い | 23 |
| yt | 未検索 / 検索できず | `yt_review` に無い / error | 0 / 0 |

この表そのものが次の手立ての優先順位になる —— own の 53 名は「公式サイトにフィードが無い」
人で、Bluesky の公開 API(§4.6)のような**別の取得器**でしか埋まらない。
yt の 7 名は許可リストを育てれば埋まり、23 名は検索語か期間窓の問題である。

## 5. スコープ外

- X / LinkedIn / Threads / TikTok のライブ取得(取得経路が塞がれている)
- **researchmap の Web API**(利用規約が JST への利用申請を求める。§4.6)
- 自動収集した英語タイトルの和訳(原文のまま掲載し、その旨をページに明記する)
- 網羅性。フィードを持たない学者・OpenAlex に載らない日本語の著作は 0 件のまま出す
- **The Conversation の著者フィード。** 著者ごとの Atom は存在するが、URL は数値 ID だけで
  解決され、スラグは無視される(実測 2026-08-31: `mark-coeckelbergh-1149861` は別人
  Leping Mou の空フィードを返した)。ID を機械的に得る経路が無いため、本プロジェクトでは使わない。
  将来使う場合は、フィードの `<title>` に本人名が入ることを採用条件にすること
