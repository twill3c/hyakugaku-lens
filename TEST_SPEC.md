# TEST_SPEC.md — hyakugaku-lens

<!-- scaffold template v1.24.0 から展開(2026-08-31)。以後このファイルはプロジェクトが育てる -->

## テスト一覧

| ID | 内容 | 対応要求 | ファイル |
|---|---|---|---|
| T-01 | データスキーマ検証: 必須キー、分類/ソース語彙、日付形式、URL 形式、各セクション最大 3 件、氏名(n)と原綴(en)の重複なし | F-01 | tests/test_data.py |
| T-02 | 名簿の構成: 100 名、日本の学者 20 名、分類 12 区分がすべて出現し `meta.cats` と集合一致、分類ごとの人数が `meta.cat_size` と一致 | F-01 | tests/test_data.py |
| T-03 | 往復一致オラクル: `out/index.html` から P / CAT_LABEL / SRC / CATS を再抽出し `data/*.json` と完全一致 | F-02 | tests/test_build.py |
| T-04 | 出力健全性: プレースホルダ残存なし、件数行(データから算出)・分類チップ 12 個の埋め込み、esc 関数と各 UI 部品の存在、外部リソース参照が無いこと | F-02, F-03, N-02 | tests/test_build.py |
| T-05 | 決定性: 2 回ビルドしてバイト一致 | N-01 | tests/test_build.py |
| T-06 | 件数行の算出: `counts_line` が人数・各セクション件数・合計をデータから導く(定数ではない)。項目を足すと行が変わることを対照で示す | F-04 | tests/test_build.py |
| T-07 | 期間絞り込みの下地: 日付「不明」を含むレコードが `sort_items` で末尾に来る。`YYYY-MM` は月初として比較される | F-05, F-07 | tests/test_merge.py |
| T-08 | マージ規則: 成功種別のみ差し替え・失敗時は既存維持(劣化継続)・日付降順(不明末尾)・URL 重複排除・最大 3 件・入力非破壊 | F-07 | tests/test_merge.py |
| T-09 | フッタ構成(フリート共通規約・画面最下部固定)と最終更新行の埋め込み | F-10 | tests/test_build.py |
| T-10 | HTML エスケープ: 引用符・山括弧を含む文字列が生の形で出力に現れない(陽性対照つき) | N-02 | tests/test_build.py |
| T-11 | 字種検査: `data/*.json` の日本語本文にキリル文字・ハングル・制御文字が混入していない。検査器の陽性対照つき | N-04 | tests/test_hygiene.py |
| T-06 | フィードパーサ煙テスト(RSS2.0 / Atom / RSS1.0、著者名の抽出)と非フィード・空フィードの例外 | F-07 | tests/test_update.py |
| T-13 | sources.json スキーマ: 実在人物への参照・種別語彙・https フィード・evidence 語彙・著者条件が要る evidence では非空・宣言と常設には理由(note)必須・人の判断で通した分が自動判定より少ないこと・(人, フィード)の組に重複がないこと | F-06 | tests/test_update.py |
| T-14 | 著者絞り込み: 共著フィードから本人の記事だけを採る。条件があるのに著者が読めない項目は捨てる。対照(混在フィードに実際に別人が入っていること・条件を外すと通ること) | F-06, F-07 | tests/test_update.py |
| T-15 | run() の劣化継続: 成功で差し替え・失敗で既存維持・skip 宣言の尊重・入力非破壊 | F-07 | tests/test_update.py |
| T-16 | フィード採用関門: 共著フィードは著者一致でしか採らない・公式サイトが下位ページのときホスト一致では採らない・別人の表題のフィードを採らない・空フィードを採らない。対照(共著であること/表題一致の誘惑が実在すること)つき | F-06 | tests/test_discover.py |
| T-17 | OpenAlex の二要素同定: 所属だけ・分野だけでは通さない、両方揃えば通す、過去の所属も手がかりにする。対照(誤同定の候補が実際に所属を共有していること)つき | F-08 | tests/test_scholar.py |
| T-18 | 業績の取り込み: DOI の絶対化・書籍の種別・題名/日付/到達先が欠けた記録の除去・問い合わせ URL が撤回論文と paratext を除いていること・劣化継続・入力非破壊・出荷分が同定済みの人だけであること | F-08 | tests/test_scholar.py |
| T-19 | robots の関門: 実際に踏んだ禁止経路を止める・許可経路は通す・途中のワイルドカード・最長一致と Allow 優先・問い合わせ文字列も照合・robots が取れないときは止めない・ホストごとに 1 回だけ引く。収集器に渡したとき実際に取得へ行かないこと(陽性/陰性対照)。`-m network` で出荷済みのフィードと公式サイト URL を実測 | N-05 | tests/test_robots.py |
| T-20 | 取得元の入れ先(sec): 既定は own・yt を宣言できる・欄どうしが干渉しない・語彙は own/yt に閉じる。declared-host は podcast かつ sec=yt で、何を確かめたかが note にあること | F-11 | tests/test_update.py |
| T-21 | 回ごとの link が無いフィード: 番組ページへ退避する・退避先も無ければ採らない。対照(その例に item/link が本当に無いこと)つき。同じ URL でも見出しが違えば別項目として残し、同じ見出しなら畳む | F-07, F-11 | tests/test_update.py, tests/test_merge.py |
| T-22 | 落としたものを数える: 理由ごとの件数が返ること(陽性対照)・全部通れば空が返ること(陰性対照)・run() が取得元ごとに記録すること | F-07 | tests/test_update.py |
| T-12 | 公式サイト URL の検証記録: `h` が非空なら `data/link_status.json` に検証済みとして記録されている(未検証の URL を出荷しない) | F-01 | tests/test_data.py |

## 実行

```bash
python -m pytest -q
```
